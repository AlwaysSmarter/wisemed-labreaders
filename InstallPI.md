<h1>Install PI</h1>

<code>sudo systemctl start systemd-timesyncd</code>

<code>sudo systemctl enable systemd-timesyncd</code>

<code>sudo apt-get -y install golang</code>

<code>sudo apt-get -y install mc</code>

<code>sudo apt-get -y remove brltty</code>

<code>sudo reboot</code>



